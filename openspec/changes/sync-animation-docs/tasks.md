## 1. NAMING.md — 配件中文名稱更新

- [ ] 1.1 S1 `has_armor`：科技護甲 → 科技鎧甲
- [ ] 1.2 S2 `has_halo`：彩虹光暈 → 彩虹光環
- [ ] 1.3 S4 `has_disco`：迪斯可球 → 迪斯可燈球
- [ ] 1.4 S5 `has_dark`：黑暗核心 → 暗黑核心
- [ ] 1.5 S6 `has_cycle_crown`：循環王冠 → 循環之冠
- [ ] 1.6 S7 `has_overfed`：過飽疊加層 → 暴食覆蓋層
- [ ] 1.7 S8 `has_frenzy`：狂熱疊加層 → 瘋狂點擊覆蓋層
- [ ] 1.8 S9 `has_bottle`：PET 瓶疊加層 → 寶特瓶覆蓋層

## 2. NAMING.md — 補充缺少細節

- [ ] 2.1 S2 `has_halo`：補入 FX3/S2 共存規則（#27/#36 期間 S2 取代 FX3，不得雙層疊加）
- [ ] 2.2 S6 `has_cycle_crown`：補入自驅邏輯四條 (a)(b)(c)(d)

## 3. ANIMATION_BRIEF.md — 交付格式修正

- [ ] 3.1 第 168 行：`.riv 原始檔，可二次編輯` → `.rev 原始檔（Rive Editor 可開啟，設計師交付）`
- [ ] 3.2 第 168 行後新增一行：`.riv Runtime 輸出檔（給前端工程師串接用）`

## 4. 手動待辦（不在 repo 範圍）

- [ ] 4.1 手動開啟 `ecoco-private/naming/ECOCO_naming_manual_v1_0_bilingual.xlsx`，修正使用說明第 2 條：`.rev` 拼寫錯誤（`.rev` 是正確的，但英文那行 `.rev files` 需確認）
