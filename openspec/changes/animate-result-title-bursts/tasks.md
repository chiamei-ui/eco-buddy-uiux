## 1. SVG 分層動畫

- [x] 1.1 在 `p2b-title.svg` 加入光暈、放射光、標題與星星的依序入場動畫
- [x] 1.2 在 `p12-title.svg` 套用相同動態節奏
- [x] 1.3 在兩份 SVG 加入 `prefers-reduced-motion` 完成狀態

## 2. 頁面整合

- [x] 2.1 移除 `.rs-title` 原有整張圖片入場動畫，避免雙重 transform

## 3. 驗證

- [x] 3.1 驗證兩份 SVG XML 可正常解析
- [x] 3.2 在瀏覽器檢查 P2b 與 P12 的分層順序與最終靜態狀態
