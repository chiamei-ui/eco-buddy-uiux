# ECO Buddy 動畫規格說明（完整版）

**版本**：v2.2
**更新日期**：2026-07-02
**適用對象**：前端工程師（@shangchian）、外包動畫師、PM
**維護人**：窗口設計師（@idahsueh-cmd）

> **資料來源**：RFP v1.2 完整版（2026-07-02）、命名手冊 v1.1  
> **相關文件**：[CHARACTER_TYPES.md](CHARACTER_TYPES.md)（36 種型態）、[MONTHLY_SPEC.md](MONTHLY_SPEC.md)（月度合約）、[NAMING.md](NAMING.md)（命名手冊）、[ANIMATION_LIST.md](ANIMATION_LIST.md)（交付清單）

---

## 一、專案概述

本專案透過 Rive 技術開發一套具備高度互動性與資料驅動（Data-driven）特性的電子寵物動態系統。核心角色採月度輪替制，每月發放一隻限定月度角色，品牌定位為有趣、永續且全齡友善。

寵物須根據後端即時傳入的數值，流暢地在多種型態組合之間切換。完整型態規格見 [CHARACTER_TYPES.md](CHARACTER_TYPES.md)，動作事件見本文件 §七，音效規格見 §八。

**設計核心原則**：基於 O2O 循環經濟的「環保行為具象化」養成體驗，透過遊戲化機制提升用戶回收黏著度。各月度角色須具備清晰的臉部識別元素，所有表情變化透過嘴巴、臉頰、身體語言呈現。風格參考：Apple / MUJI 極簡設計語彙。

---

## 二、月度角色制

### 核心邏輯

系統每月 1 日自動發放當月新角色，用戶於月內進行養成互動。月末結算後，角色依最終三大屬性數值組合決定收藏等級並歸檔，同時推播下月角色預告。

- 每月角色外觀設計不同，但狀態機命名規範、插槽架構與事件埋點維持完全一致
- 角色發放初始數值：`hp_level 50 / clean_level 50 / mood_level 50`（即預設型態 #14「環保初心」）

### 單一骨架架構原則

每月新角色採用相同骨架架構，僅替換外觀 Skin。Phase 1 建立的骨架必須具備足夠彈性，以支援後續每月不同外觀的 Skin 替換。

**Phase 1 重點**：建立可參數驅動、可月度 Skin 替換的骨架地基。骨架品質直接決定後續所有月度角色的製作效率。

Phase 0B（對齊期）須由外包以書面提出骨架具體拆分方案、臉部識別元素縮放規則及命名對齊，同時確認 Rive Editor 版本號與對應 Runtime 版本號。RFP v1.2 新增三項 Phase 0B-2 書面確認要求，須全部取得甲方書面確認後方可進入 Phase 2：（C-0B2-1）3.6 元素圖層拆分計畫；（C-0B2-2）State Machine 名稱 `BuddyMachine` 與 input 命名確認；（C-0B2-3）所有 `ev_*` trigger 逐一觸發命名對齊測試。

---

## 三、核心技術架構

### 3.1 開發原則

- 採「單一骨架（Single Skeleton）」搭配「狀態機（State Machine）」與「插槽系統（Slot System）」
- 嚴禁為各別型態建立獨立檔案，所有型態切換必須在單一 `.rev` 原生檔案中實作
- 優先使用變換約束（Transform Constraints）減少手動關鍵影格，提升可維護性
- 所有變數與事件命名必須嚴格遵守命名手冊 v1.1（大小寫與底線須完全一致）
- 開發啟動前須書面確認 Rive Editor 版本號及對應 Runtime 版本號

### 3.2 三大核心屬性參數

後端以整數（0–100）傳入三個參數，分三個階層觸發視覺變化。後端若傳入非整數值，Rive 狀態機須以 `floor()` 無條件捨去後再判斷。

**階層定義**：低（Low）：0–30　｜　中（Mid）：31–65　｜　高（High）：66–100

#### hp_level — 體型縮放

| 參數名稱 | 類型 | 數值範圍 |
|---------|------|---------|
| `hp_level` | Number | 0–100 |

| 階層 | 數值 | 視覺效果 |
|------|------|---------|
| 低 | 0–30 | 視覺呈現偏低，以扁薄、姿態下沉、低能量輪廓傳達虛弱體態；臉部核心識別元素比例須保持固定 |
| 中 | 31–65 | 標準比例基準，線條俐落；臉部核心識別元素比例須保持固定 |
| 高 | 66–100 | 視覺量感約為 mid 基準之 105–115%，以姿態挺立、輪廓飽滿傳達健康活力；臉部核心識別元素比例須保持固定 |

各階層內固定縮放，階層間以 State Machine 過渡動畫切換，**嚴禁**以 1D Blend Axis 線性映射實作。

#### clean_level — 潔淨外觀

| 參數名稱 | 類型 | 數值範圍 |
|---------|------|---------|
| `clean_level` | Number | 0–100 |

| 階層 | 數值 | 視覺效果 |
|------|------|---------|
| 低 | 0–30 | 滿布髒污粒子，色彩飽和度降低 40%（低階內部線性過渡：level=0 時 −40%，level=30 時 0%；此為本專案唯一允許的 1D 連續映射例外） |
| 中 | 31–65 | 霧面乾淨材質，色彩還原 100% |
| 高 | 66–100 | 材質趨於輕盈透亮，晶透折射質感；含發光光暈及微粒子漂浮 |

#### mood_level — 心情表情

| 參數名稱 | 類型 | 數值範圍 |
|---------|------|---------|
| `mood_level` | Number | 0–100 |

| 階層 | 數值 | 視覺效果 |
|------|------|---------|
| 低 | 0–30 | 眉毛下垂、嘴角向下、姿態下沉 |
| 中 | 31–65 | 中性表情、規律眨眼、微幅律動 |
| 高 | 66–100 | 瞳孔放大、嘴角上揚、愛心粒子 |

依 mood_level 三段階層以 Rive State Machine 離散切換，**嚴禁**以 1D Blend Axis 線性映射實作。

> 三個參數各分三階層，3×3×3 共構成 27 種基礎型態。後端串接與 Rive Runtime 呼叫由甲方工程師自行實作。

### 3.3 預設型態規格（#14「環保初心」）

| 參數 | 預設值 | 視覺說明 |
|------|-------|---------|
| `hp_level` | 50 | 標準體型 100%，線條俐落 |
| `clean_level` | 50 | 霧面乾淨材質，高質感 |
| `mood_level` | 50 | 中性表情，規律眨眼，溫和微笑 |
| 整體風格 | — | 乾淨霧面高質感；Apple / MUJI 極簡設計語彙；平靜呼吸，溫和微笑 |

Phase 1 驗收時須能正確呈現此預設型態，並提供 `.riv` 發布檔供工程師進行 Runtime 初步串接測試。

### 3.4 Idle 待機循環規格

三種 Idle 循環依 mood_level 階層自動切換。所有 Idle 須為無縫循環（Seamless Loop），首尾影格銜接自然，不得有跳動或停頓感。

| Idle 類型 | 循環時長 | 動作描述 |
|----------|---------|---------|
| 低 mood Idle | 5 秒 | 偶爾嘆氣 ＋ 耳朵下垂；整體姿態低沉，動作幅度小 |
| 中 mood Idle | 4 秒 | 規律眨眼 ＋ 偶爾回頭張望；自然待機狀態 |
| 高 mood Idle | 3 秒 | 左右輕微搖擺 ＋ 哼歌嘴型；活潑有節奏感 |

### 3.5 臉部識別元素固定規則

角色臉部核心識別元素（眼睛、腮紅、嘴巴）在 `hp_level` 極值縮放時，比例須保持固定，**不得**跟隨骨架等比縮放而變形。縮放規則須於 Phase 0B 骨架拆分方案中由外包提出，並經甲方書面確認。

### 3.6 狀態專屬視覺元素規則（RFP v1.2 新增）

每一個基礎型態（#01–#27，#14 除外）各有一組「狀態專屬視覺元素」，作為該型態的視覺身份識別，共 **26 個狀態項目**。

**基本規則：**
- 由 Rive State Machine 依 hp_level／clean_level／mood_level 解析後的當前基礎狀態自動控制顯示／隱藏
- **不使用 `has_*` Boolean，後端不傳值**
- 元素附著於骨架節點，隨骨架移動，**不得具有獨立循環動畫、粒子發射或專屬 timeline**
- 命名格式：`state_<NN>_<type>_<descriptor>`；type 限 `prop`（道具）、`decor`（裝飾飾品）、`decal`（貼花）、`mark`（符號標記）
- 完整 26 個狀態規格見 [NAMING.md §四](NAMING.md) 及命名手冊 v1.1「State-Specific Visual Elements」分頁

**跨月度沿用規則：**
- （b-1）預設沿用首版美術造型，月度設計師調整位置與縮放比例
- （b-2）若月度角色風格不適合直接沿用，得於月度合約附件另約局部重繪，但不得新增後端 input、不得變更 State Machine、不得改變 26 個狀態項目數量

**特殊型態覆蓋優先序（決定 3.6 元素是否顯示）：**

| 優先序 | 類型 | 觸發條件 | 3.6 元素 |
|---|---|---|---|
| g-1（最高）| 取代型 | `has_dark=true` | 全部隱藏 |
| g-2 | 全身覆蓋型 | `has_bottle / has_armor / has_golden=true` | 全部隱藏 |
| g-3 | 頭頂飾品型 | `has_laurel / has_disco / has_halo / has_cycle_crown=true` | 頭頂位置隱藏，其他並存 |
| g-4（最低）| 疊加型 | `has_overfed / has_frenzy=true` | 全部並存 |

**Phase 0B-2 須提交（C-0B2-1）：** 26 個狀態逐一清單、主命名、預計子圖層、骨架附著位置、與 S1–S10 及 FX 可能衝突說明，甲方書面確認後方可進入 Phase 2。

### 3.7 月度替換架構鎖定規則（RFP v1.2 新增）

月度替換**只能更換 Skin（視覺素材層）**，不得變更控制層。以下項目首版建立後鎖定：

| 鎖定項目 | 說明 |
|---|---|
| State Machine 名稱 | `BuddyMachine`，固定不得改名 |
| Core Input 命名 | `hp_level` / `clean_level` / `mood_level`，範圍 0–100，不得新增或改名 |
| 事件命名 | 所有 `ev_*` trigger 命名鎖定，不得新增或改名 |
| Rive 內部型態對應 | `morph_01`–`morph_27` 為 Rive 內部識別名，**非後端 input**，後端不傳送此類變數 |
| Pivot / Bounds / Draw Order | 首版 Phase 0B-2 確立基準，月度可在安全範圍內微調，不得造成位移錯位或遮擋錯亂 |

**月度合約驗收繼受：** C-P2-2（骨架附著律動）、C-P2-3（狀態切換轉場）、C-P2-6（pivot／bounds／draw order 安全範圍）需在每次月度替換時重新檢核。

---

## 四、效能門檻與壓力測試規格

### Phase 1 基礎要求

| 項目 | 要求 |
|------|------|
| Rive Editor 版本 | Phase 0B 對齊期間書面確認版本號；版本升級須事先獲甲方書面確認 |
| 檔案格式 | 100% 向量路徑（Vector Paths），嚴禁使用點陣圖 |
| 最終發布檔 `.riv` | < 120KB（Phase 1 試作即須符合） |
| 快速變量測試 | 1 秒內 hp_level / clean_level / mood_level 各自重複切換 0↔100，不得產生破面或異常形變 |
| 佈局設定 | Rive Layout 響應式佈局；暫定展示框比例 3:4（寬:高）；角色座標原點固定底部中心；參考裝置 iPhone 15（390pt）及 Android（360pt） |

### Phase 2–4 完整壓力測試規格（RFP §8.2）

| 測試類別 | 測試方法與通過標準 |
|---------|-----------------|
| 檔案體積 | 最終發布檔（.riv）須控制在 120KB 以內 |
| 記憶體監控 | Runtime 執行期間，記憶體堆疊增加量不得超過 15MB |
| 快速變量測試 | 1 秒內各自獨立重複切換 hp_level / clean_level / mood_level 至極值（0 與 100 交替），不得產生破面或異常形變 |
| 高頻觸發測試 | 一分鐘內對主要動作事件各重複觸發 20 次，觀測 CPU 使用率是否異常飆升 |
| 長時間運作 | 連續播放 Idle 動畫 60 分鐘，不得有記憶體洩漏或閃退現象 |
| 佈局響應式 | 於各螢幕比例（19.5:9 / 19.6:9 / 20:9）測試，確認角色座標原點固定於底部中心，無裁切異常 |

---

## 五、Phase 1 驗收項目

| 編號 | 驗收項目 | 等級 |
|------|---------|------|
| A-1 | 交付 .rev 原始檔，且包含層級分明的圖層命名 | 必要 |
| A-2 | 所有型態變化在單一 .rev 檔案中透過狀態機與插槽邏輯實作（嚴禁建立獨立檔案） | 必要 |
| A-3 | 交付 .riv 發布檔（含 #14「環保初心」預設型態之初版）供工程師 Runtime 初步串接測試；須符合 < 120KB | 必要 |
| B-1 | hp_level（0–100）依三段階層離散切換：低階扁薄下沉、中階標準基準、高階 105–115% 挺立飽滿；以 State Machine 過渡，非 1D Blend Axis | 必要 |
| B-2 | clean_level（0–100）依三段階層離散切換：低階髒污粒子＋飽和度低階內線性過渡、中階霧面、高階晶透＋光暈；非 1D Blend Axis | 必要 |
| B-3 | mood_level（0–100）依三段階層離散切換：各階層對應 Idle 循環；以 State Machine 過渡，非 1D Blend Axis | 必要 |
| B-4 | 臉部核心元素（眼睛、腮紅、嘴巴）在 hp_level 極值切換時，比例保持固定不變形 | 必要 |
| B-5 | 三種 Idle 待機循環（低 / 中 / 高 mood）完整實作，且為無縫循環 | 必要 |
| B-6 | 快速變量測試（1 秒內 0↔100 切換）無破面或異常形變 | 必要 |
| B-7 | 命名規範對照截圖完整提交，且與命名手冊 v1.0 完全一致 | 必要 |

> 所有「必要」項目須全數通過，方視為 Phase 1 試作驗收合格。

---

## 六、Phase 1 範圍備注

Phase 1 試作不要求實作以下規格。正式合約（Phase 2–4）啟動後，相關規格已收錄於以下文件：

| RFP 章節 | 內容 | 文件位置 |
|---------|------|---------|
| §4 | 36 種型態完整規格（27 基礎型態 + 9 特殊隱藏型態） | [CHARACTER_TYPES.md](CHARACTER_TYPES.md) |
| §5 | 特殊配件插槽規格（S1–S10 Boolean 插槽、連動邏輯） | [CHARACTER_TYPES.md §三](CHARACTER_TYPES.md) + [NAMING.md §二](NAMING.md) |
| §6 | 動畫互動框架（10 大動作事件） | 本文件 §七 |
| §7 | 互動事件與音效規範 | 本文件 §八 |
| §9 | 完整交付物清單（含粒子效果清單） | [ANIMATION_LIST.md](ANIMATION_LIST.md) |
| §2.3、§11 | 月度合約交付規格 | [MONTHLY_SPEC.md](MONTHLY_SPEC.md) |

---

## 七、動作事件規格（RFP §6.2）

> 10 大核心動作事件完整規格。事件埋點命名見 [NAMING.md §三](NAMING.md)。

| 動作事件 | 觸發條件 | 影響屬性 | 動畫表現摘要 | 事件埋點 |
|---------|---------|---------|------------|---------|
| 餵食 Feeding | 掃描食物 QR Code | HP +N | 食物掉落→張嘴→吞嚥→膨脹→滿足（1.5s）；W4 稀有食物觸發 ev_gold_burst 金色粒子爆發 | ev_eat_gulp, ev_eat_chew, ev_gold_burst (W4) |
| 清潔 Cleaning | 回收品質良好 | 潔淨 +N | 品牌橘掃描光→汙漬剝落→高光 sweep→甩毛（2.0s） | ev_scan_start, ev_dirt_pop, ev_scan_end |
| 使用道具 Item Use | 商城購買後使用 | 心情 +N | 禮盒掉落→拆開閃光→互動→愛心噴發（2.0s） | ev_item_open, ev_item_react |
| 觸摸互動 Touch | 點擊寵物身體 | 心情 +1 | 低=閃躲、中=眨眼漣漪、高=蹭手撒嬌（0.8s）；冷卻 0.5s（Rive 端 State Machine 內建 timer），每日上限 20 次（後端計數） | ev_pet_react, ev_pet_jump |
| 觀看廣告 Ad Reward | 主動點擊獎勵廣告 | 心情 +5 | 品牌橘聚光燈→充能粒子→角色發光（1.5s） | ev_ad_charge, ev_ad_glow |
| 每日衰減 Decay | 24h 未登入（以最後一次登入時戳為準） | HP-3 潔淨-5 心情-5 | 冷顫→縮小→委屈表情看鏡頭→新 Idle（1.5s）；登入後播放一次，不依未登入天數重複播放 | ev_decay_start, ev_decay_look, ev_decay_end |
| 進化/跨階 Evolution | 屬性跨越 30/65 門檻 | 觸發型態重算 | 白光包裹→漸變新型態→品牌色粒子爆發（1.5s）；觸發 cooldown 2.0s，期間後續跨階事件排隊（Queue）順序播放，**不得並行** | ev_evolve_start, ev_evolve_burst |
| 月初登場 Monthly Spawn | 每月 1 日系統發放 | — | 自蛋中孵化 / 自光中出現（2.0s） | ev_spawn_hatch |
| 月底結算 Month-End | 月末最後一日 | — | 揮手告別→走入圖鑑相框→下月角色預告（2.0s） | ev_farewell_start, ev_farewell_end |
| 壞滅崩潰 Collapse | 三大屬性同時歸零（hp=0、clean=0、mood=0）後首次登入 | — | 黑色龜裂紋蔓延→紅光眼睛亮起→壞滅核心型態（2.0s）；強制開啟 S5 has_dark=true，鎖定至月底；與每日衰減互斥，後端負責判斷；後端須於屬性全歸零時停止衰減結算（以 0 為下限）並標記「壞滅待觸發」 | ev_collapse_start, ev_collapse_end |

> ※ 進化 Queue 機制：Rive State Machine 在 2.0s cooldown 期間將後續跨階事件排隊順序播放，不並行。  
> ※ 衰減動畫僅於登入後觸發一次（累積呈現）；三大屬性同時歸零後登入直接觸發壞滅崩潰，不播衰減。

---

## 八、音效規格（RFP §7）

**格式規格**：MP3、採樣率 44.1kHz 以上  
**授權要求**：須附原廠商業使用授權憑證（PDF 原件）；授權地區須明確涵蓋**台灣**及 **App Store / Google Play 全球發行**

| 觸發動作 | 事件埋點 | 音效描述 | 時長範圍 |
|---------|---------|---------|---------|
| 餵食（W1–W3 一般食物） | ev_eat_gulp / ev_eat_chew | Q 彈咀嚼聲 + 吞嚥聲 | 0.4–0.6s |
| 餵食（W4 稀有食物） | ev_gold_burst | 金幣灑落音效 + 短促上升音效（成就感） | 0.5–1.0s |
| 清潔 | ev_scan_start / ev_dirt_pop / ev_scan_end | 品牌科技掃描音（橘光劃過）+ 塵污爆開聲 | 0.3–0.8s |
| 觸摸互動 | ev_pet_react / ev_pet_jump | 親民可愛叫聲或環境音；音色依心情階層有差異 | 0.2–0.5s |
| 進化 | ev_evolve_start / ev_evolve_burst | 品牌色粒子爆發上升音效，具成就感與儀式感 | 1.0–2.0s |
| 每日衰減 | ev_decay_start / ev_decay_look / ev_decay_end | 冷顫縮小音效 + 委屈輕嘆氣聲 | 0.3–0.8s |
| 使用道具 | ev_item_open / ev_item_react | 開箱閃光音效 + 依道具類型不同的互動音效 | 0.5–1.5s |
| 觀看廣告 | ev_ad_charge / ev_ad_glow | 充能上升音效 + 角色發光收尾音效 | 0.3–0.8s |
| 壞滅崩潰 | ev_collapse_start / ev_collapse_end | 龜裂擴散低頻音效 + 紅光眼睛亮起低沉重擊 | 0.5–1.0s |
| 月初登場 | ev_spawn_hatch | 孵化破殼音效，帶有期待感與新生感 | 1.0–2.0s |
| 月底結算 | ev_farewell_start / ev_farewell_end | 溫馨告別音效，帶有不捨與期待下月感 | 1.0–2.0s |

> ※ 共 22 個 ev_ 事件音效（含 W4 限定 ev_gold_burst），與驗收表 E-1 對應。乙方須提供完整 22 支音效素材包。

---

## 九、完整驗收項目（Phase 2–4）

### Phase 0B-2 新增驗收項目（RFP v1.2）

> 三項須全部取得甲方書面確認後，外包方可進入 Phase 2 正式製作。

| 編號 | 驗收項目 | 等級 | 階段 |
|------|---------|------|------|
| C-0B2-1 | 3.6 元素圖層拆分計畫確認：乙方提交 26 個狀態項目逐一清單，含主命名、預計子圖層、骨架附著位置（對應實際 Rive 節點命名）、與 S1–S10 插槽及 9.1 FX 可能衝突說明。通過標準：甲方書面確認。 | 必要 | Phase 0B-2 |
| C-0B2-2 | State Machine 名稱與必要 input 確認：（1）SM 名稱 `BuddyMachine`；（2）`hp_level` / `clean_level` / `mood_level`（Number, 0–100）；（3）與命名手冊 v1.1 完全一致，無誤植。通過標準：甲方書面確認。 | 必要 | Phase 0B-2 |
| C-0B2-3 | 事件 Trigger 命名對齊測試：對命名手冊 v1.1「Events」分頁所有 `ev_*` trigger 逐一觸發測試。通過標準：名稱與命名手冊完全一致；App 端呼叫時能正確觸發對應動畫；無靜默失敗。 | 必要 | Phase 0B-2 |

### C 組：特殊型態、配件、3.6 元素（Phase 2–3）

#### Phase 2：3.6 狀態專屬視覺元素驗收

| 編號 | 驗收項目 | 等級 | 階段 |
|------|---------|------|------|
| C-P2-1 | 3.6 元素顯示測試：對 #01–#27（#14 除外）逐項切換屬性值，確認對應 3.6 元素正確顯示，命名符合 3.6(e) 與命名手冊 v1.1。 | 必要 | P2 |
| C-P2-2 | 3.6 元素附著與骨架跟隨測試：Idle、餵食、清潔、觸摸、廣告、衰減動作期間，元素跟隨骨架節點移動，不漂浮錯位、不獨立播放額外動畫、不遮蓋臉部核心識別元素。 | 必要 | P2 |
| C-P2-3 | 3.6 元素狀態切換與進化轉場測試：（a）一般跨階切換：元素 0.1–0.2s 淡出淡入或直接切換，不播 1.5s 進化動畫；（b）`ev_evolve_start` 觸發：舊狀態元素 0.2s 淡出 → 過渡期全隱藏 → 新狀態元素 0.2s 淡入後接 Idle。 | 必要 | P2 |
| C-P2-4 | 3.6 元素壓力測試：固定 clean/mood 於 Low/Mid/High，hp 0↔100 快速切換（1 秒內），9 組對應（#01↔#19、#02↔#20…#09↔#27）。通過標準：無破面、無異常形變、無附著節點脫離。 | 必要 | P2 |
| C-P2-5 | .riv 檔案大小驗證：含全部 26 個 3.6 狀態項目時，.riv 發布檔 < 120KB。 | 必要 | P2 |
| C-P2-6 | Pivot / Bounds / Draw Order 安全範圍測試（首版驗收）：確立各部件 pivot 基準、bounds 安全範圍、draw order 邏輯，作為月度替換基準。通過標準：月度替換時不發生位移、裁切跳動、遮擋錯位。 | 必要 | P2（首版） |

#### Phase 3：特殊插槽與配件

| 編號 | 驗收項目 | 等級 | 階段 |
|------|---------|------|------|
| C-1 | 10 種特殊插槽（S1–S6 裝飾配件、S7–S10 外觀覆蓋）是否能各自單獨透過對應 Boolean 觸發測試？多個 `has_*` 同時觸發時，是否依 §5.2 成就型插槽互斥規則、#27／#36 連動規則與 #33 鎖定規則正確顯示？ | 必要 | P3 |
| C-2 | 配件層是否在骨架變型時正確律動，不產生破面？ | 必要 | P3 |
| C-3 | W4 稀有食物掉落時，ev_gold_burst 觸發後是否正確顯示 FX4 金色粒子爆發視覺，並於播放完畢自動歸零？ | 必要 | P3 |
| C-4 | 特殊型態 #33（壞滅核心）之 has_dark 插槽，是否具備獨立暗黑龜裂紋理與紅光眼睛？ | 必要 | P3 |
| C-5 | 月底結算後，後端是否將所有特殊插槽重置為 false，並將三大屬性重置至 50？（後端責任，乙方配合驗收確認） | 必要 | P4 |
| C-6a | 在所有 has_* 均為 false 之初始狀態下，後端僅送 has_cycle_crown=true（不另送 has_halo），確認 S2 彩虹光環正確呈現（驗證 Rive 自驅 (a) 條）。驗收須從 has_cycle_crown=false 起主動寫入 =true，觸發一次明確之 false→true edge。 | 必要 | P3 |
| C-6b | 延續 C-6a 狀態，後端同時送 has_cycle_crown=false 與 has_halo=false，確認 S2 消失無殘留；再測另一情境：has_cycle_crown=true 且 has_halo=true 下，後端送 has_cycle_crown=false 與 has_halo=true（顯式保留），確認 S2 仍維持顯示（驗證 (b)(c) 條）。 | 必要 | P3 |
| C-7 | #25 悲傷神獸型態觸發時，FX6 發光眼淚粒子是否正確生成於眼角，並與 H-H-L 莊嚴氛圍同步？ | 必要 | P3 |

#### Phase 3：3.6 元素 × 特殊插槽覆蓋規則驗收

| 編號 | 驗收項目 | 等級 | 階段 |
|------|---------|------|------|
| C-P3-1 | 成就型 `has_*` 互斥測試：從 #14 起，依序切換 has_overfed → has_bottle → has_armor → has_dark，每次前一視覺須完全消失，無殘留圖層、無雙重渲染。 | 必要 | P3 |
| C-P3-2 | 3.6 元素覆蓋規則組合測試：對含 3.6 元素之基礎狀態，逐一測試 g-1（has_dark 全隱）、g-2（has_bottle 全隱）、g-3（has_disco 頭頂隱藏）、g-4（has_overfed 並存），並測組合情境（如 has_bottle+has_halo → 全隱）。通過標準：符合 g-1 > g-2 > g-3 > g-4 優先序。 | 必要 | P3 |
| C-P3-3 | `has_halo` / `has_cycle_crown` 連動測試：確認兩者不受 §5.2 互斥規則約束，互斥族群切換時 has_halo / has_cycle_crown 狀態不受影響。 | 必要 | P3 |

### D 組：動畫事件（Phase 2）

| 編號 | 驗收項目 | 等級 | 階段 |
|------|---------|------|------|
| D-1 | 10 大核心動作事件（餵食、清潔、道具、觸摸、廣告、衰減、進化、壞滅崩潰、月初登場、月底結算）是否完整實作？ | 必要 | P2 |
| D-2 | 餵食動畫（1.5s）各步驟時間軸是否符合規格（食物掉落→張嘴→吞嚥→膨脹→滿足）？ | 必要 | P2 |
| D-3 | 壞滅崩潰動畫（2.0s）是否正確觸發，ev_collapse_start / ev_collapse_end 是否與關鍵影格同步？ | 必要 | P2 |
| D-4 | 觸摸互動冷卻機制（0.5s 防連點）是否實作？Rive 端動畫事件是否可重複觸發？ | 必要 | P2 |
| D-5 | 觸摸部位差異（頭/肚/尾）是否有獨立反應動畫？ | 加分 | P2 |
| D-6 | 月底告別動畫（2.0s）是否正確觸發，ev_farewell_start / ev_farewell_end 是否與動畫開始及結束關鍵影格同步？ | 必要 | P2 |

### E 組：事件埋點與音效（Phase 2/4）

| 編號 | 驗收項目 | 等級 | 階段 |
|------|---------|------|------|
| E-1 | 所有規格音效事件（共 22 個 ev_ 事件，包含 ev_gold_burst）是否已在 Rive 編輯器中埋入並與關鍵影格同步？ | 必要 | P2 |
| E-2 | 音效素材是否附帶商業使用授權憑證（PDF 原件）？授權地區是否明確涵蓋台灣及 App Store / Google Play 全球發行？ | 必要 | P4 |
| E-3 | 開發命名手冊（Mapping List）是否已交付，供甲方工程師自行實作 Rive Runtime 串接？ | 必要 | P4 |
