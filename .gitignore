# 本機私有文件，不進 repo
CLAUDE.local.md
ecoco-private/

# Claude Code 個人本地設定（每人角色不同，不共用）
.claude/settings.local.json

# 大型二進位媒體（> 5MB，留雲端）
*.mov
*.mp4

# 靜態視覺定案稿（本機留存，不進 repo）
assets/animations/drafts/*.pdf

# Playwright
node_modules/
playwright-report/
playwright-screenshots/
test-results/
